## Since Last Meeting

* [Report](https://github.com/VisruthSK/loo-parallelization/blob/12cb032457cd2875f0dc5b8ac6193010b4e67ed0/docs/report.pdf):
    * Extended some discussion surrounding `mori`--especially see [section 4.3.a](https://github.com/VisruthSK/loo-parallelization/blob/12cb032457cd2875f0dc5b8ac6193010b4e67ed0/docs/report.pdf).
* Profiled `loo::loo()`, saved results into `./profiles`. The recently released [`debrief`](https://opensource.posit.co/blog/2026-06-22_debrief-0-1-0/) package can be used to analyze our results further.
* `posterior`
    * Generated sorting routine in C to replace hot path, see [#453](https://github.com/stan-dev/posterior/pull/453). Walltime speedup of about 1.5-2x, use 1/2 - 1/5th the memory as well. Haven't thoroughly reviewed the C code, but tests pass. Before merging, there should be stricter tests, and someone who knows C should review the PR to see if there are any ways to eke out performance or write more idiomatic code.
    * Wrote a helper function for `loo` to use instead of plain `ps_tail()` in [#452](https://github.com/stan-dev/posterior/pull/452). This continues from discussion last week.
    * Assorted other changes focused on getting CI/CD green in [#454](https://github.com/stan-dev/posterior/pull/454).

## Questions for Meeting

### How to use `mori` properly as a package owner? (with `mirai`)

I haven't gone through GH to find pakcage swhich are using `mirai` for paralellization and also use `mori`, but the main question I am have is how to use `mori` opportunistically to avoid copying shared objects to all workers in `mirai`. If all deamons are remote then `mri` cannot be used, but if there are mixed remote and local daemons then it should technically be possible to share the common objects to just the local deamons and copy them over to the remote. The main blocker here is finding a solution which is clean and automatic, instead of relying on on the users to specify where where their compute resides.

## Meeting Notes

## Next Steps
