## Since Last Meeting

* Working on refactoring [report](https://github.com/VisruthSK/loo-parallelization/blob/bae3f32dfa0b64102a49a6836ac7466a9901166d/docs/report.pdf) still (thanks Florence!)
    * Section 3.1.1 on reproducibility with `mirai` can be reworked into docs eventually?
    * Section 4.3 on `mori` model methods experiment, as expected, doesn't work
    * Florence added a helpful glossary in the appendix which will be maintained and can be reworked into docs eventually
    * Florence also added neat introduction/motivation, which can can be reworked into docs eventually
* Opened PRs for vectorization: [#368](https://github.com/stan-dev/loo/pull/368) on Bayesian Bootstrap and [#370](https://github.com/stan-dev/loo/pull/370) on log score gradient
* Opened PR for basic parallelization fixes: [#371](https://github.com/stan-dev/loo/pull/371)

## Questions for Meeting

How to deal with `mc.cores` option and `cores` arguments? 

Update `loo_cores()` validation helper, and change defaults from `cores = getOption("mc.cores", 1)` to `cores = 1`, or to `NULL` (i.e. default to serial or parallel execution?)?

**Old:**

```r
# Check how many cores to use and throw deprecation warning if loo.cores is used
loo_cores <- function(cores) {
    loo_cores_op <- getOption("loo.cores", NA)
  if (!is.na(loo_cores_op) && (loo_cores_op != cores)) {
      cores <- loo_cores_op
    warning("'loo.cores' is deprecated, please use 'mc.cores' or pass 'cores' explicitly.",
            call. = FALSE)
  }
  return(cores)
}
```

**Rough Proposal:**

```r
# Check how many cores to use and throw deprecation warning if loo.cores is used
loo_cores <- function(cores) {
  # TODO: link parallelization vignette
  if (!is.na(getOption("loo.cores", NA))) {
    warning(
      "'loo.cores' is deprecated, please remove 'loo.cores' and use mirai::daemons() for parallelization. See the parallelization vignette for more details.",
      call. = FALSE
    )
  }
  if (is.null(cores)) {
    if (!requireNamespace("mirai", quietly = TRUE)) {
      stop(
        "`cores = NULL` requires the 'mirai' package. Please install `mirai`, call `mirai::daemons(n)`, and then call loo with `cores = NULL`. See the parallelization vignette for more details.",
        call. = FALSE
      )
    }
    # mirai_map() when no daemons are set says:
    #
    #> Error in `mirai::mirai_map()`:
    #> ! No daemons set.
    #> Use e.g. mirai::daemons(6) to set 6 local daemons.

    # but we could also do our own check
    # if (!mirai::daemons_set()) {
    #   stop(
    #     "`cores = NULL` requires `mirai` daemons to be set. Please call `mirai::daemons(n)` before calling loo with `cores = NULL`. See the parallelization vignette for more details.",
    #     call. = FALSE
    #   )
    # }

    return(NULL)
  }

  if (cores != 1) {
    stop(
      "`cores` no longer controls parallelization. Please call mirai::daemons(",
      cores,
      ") before calling loo, and don't pass `cores`. See the parallelization vignette for more details.",
      call. = FALSE
    )
  }

  cores
}
```

New `loo_cores()` maintains contract that `cores` should be `1` for serial execution, and `NULL` for parallel execution.

## Meeting Notes

`loo` object can be shared with `mori` maybe?

Removing `cores` and using only `mirai::daemons()` would be a major breaking change. Should coordinate with the measure refactor to get to v3.0.0.

Finalize user API later, after core parallelization details are completed.

### Non parallelization notes: 

PRs #371, 370 and 368 are small and ready to reviewed.

We are waiting for authors to reply for #369 and #328; will solve those issues in-house if they don't reply.

Waiting for #340 author to have time to respond.

#291 needs to be recreated for v3.

#290 is waiting on author to make changes in `posterior`.

#185 might be useful, though maybe could use [some C++](https://search.r-project.org/CRAN/refmans/DescTools/html/Large.html). This could go with issue #260 about using C++ for PSIS. Maybe collate performance improvements, see if adding Rcpp nets roughly an order of performance.

#178 needs to be modified--add tests, fix example, name, etc.

## Next Steps

Put vectorization PRs benchmarks in the report

Look into how `loo` object can be shared with `mori`--should be okay.

Can you share pointers to CPP?

```r
shared <- mori::share(list(fit = cmdstan_fit))
```

This works? Look into this.
